package cn.projectan.strix.utils;

/**
 * @author ProjectAn
 * @date 2021/6/11 18:14
 */
public class NumUtils {

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Integer i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Long i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Short i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Byte i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Float i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于0
     */
    public static boolean isPositiveNumber(Double i) {
        return i != null && i > 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Integer i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Long i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Short i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Byte i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Float i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且大于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且大于等于0
     */
    public static boolean isNonNegativeNumber(Double i) {
        return i != null && i >= 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Integer i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Long i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Short i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Byte i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Float i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于0
     */
    public static boolean isNegativeNumber(Double i) {
        return i != null && i < 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Integer i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Long i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Short i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Byte i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Float i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且小于等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且小于等于0
     */
    public static boolean isNonPositiveNumber(Double i) {
        return i != null && i <= 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Integer i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Long i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Short i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Byte i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Float i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且等于0
     */
    public static boolean isZero(Double i) {
        return i != null && i == 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Integer i) {
        return i != null && i != 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Long i) {
        return i != null && i != 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Short i) {
        return i != null && i != 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Byte i) {
        return i != null && i != 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Float i) {
        return i != null && i != 0;
    }

    /**
     * 判断 Number 是否非空且不等于0
     *
     * @param i 需要判断的值
     * @return 是否非空且不等于0
     */
    public static boolean isNotZero(Double i) {
        return i != null && i != 0;
    }

}
